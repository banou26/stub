/node_modules
/.env
/test-results
/build
# the release fkn-sign writes in CI for the version being published, never a file of the tree
/fkn.json
/dist-seed
/corpus
/src/generated
/examples/source-plugin/dist
/examples/source-plugin/node_modules

.playwright-mcp/

# the graph-engine benchmark rig output (vite.bench.config.ts)
/bench-out
